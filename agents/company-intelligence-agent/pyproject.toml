[project]
name = "company-intelligence-agent"
version = "0.1.0"
description = "Persistent portfolio and company monitoring for HyperVision"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
company-intel = "company_intel.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

